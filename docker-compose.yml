services:
  neuroauto-app:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: neuroauto-app
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
